/* Sequence is used as a timestamp, therefore ORDER is required
 * to guarantee correct id creation in RAC environments
 * (although this will slow down logging a bit)
 */
create sequence pit_log_seq order cache 10000;